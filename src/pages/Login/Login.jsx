import React from "react";
import LoginForm from "../../components/LoginForm/LoginForm";
const LogIn = () => {
  return (
    <section>
      <LoginForm />
    </section>
  );
};

export default LogIn;
